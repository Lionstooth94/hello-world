# hello-world
STELLAR_VALUE_EMPTY_TX_SET is a Stellar consensus value that represents a ledger close with an explicitly empty transaction set.

It indicates validators agreed to close the ledger without including any transactions, rather than referencing a missing or invalid transaction set.
